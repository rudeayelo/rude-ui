import { Story, Meta, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, radios, text } from "@storybook/addon-knobs";
import {
  EmailIcon,
  DeleteIcon,
  MenuIcon,
  ExpandBottomIcon,
  RefreshIcon,
} from "../helpers/icons";
import { baseTheme, Flex, Text, Button, IconButton } from "../src";

<Meta title="MDX|Button" component={Button} />

# Button

<Props of={Button} exclude="theme" />

## Appearances

<Preview>
  <Story name="Appearance">
    <Flex>
      <Button>Default</Button>
      <Button appearance="primary" ml={3}>
        Primary
      </Button>
      <Button appearance="minimal" ml={3}>
        Minimal
      </Button>
    </Flex>
  </Story>
</Preview>

## Size

<Preview>
  <Story name="Size">
    <Flex>
      <Button size="large">Large</Button>
      <Button ml={3}>Default</Button>
      <Button size="small" ml={3}>
        Small
      </Button>
    </Flex>
  </Story>
</Preview>

## Tone

<Preview>
  <Story name="Tone">
    <Flex>
      <Button>Default (info)</Button>
      <Button tone="neutral" ml={3}>
        Neutral
      </Button>
      <Button tone="positive" ml={3}>
        Positive
      </Button>
      <Button tone="critical" ml={3}>
        Negative
      </Button>
      <Button tone="caution" ml={3}>
        Caution
      </Button>
    </Flex>
  </Story>
</Preview>

## Mixing appearance and tone

<Preview>
  <Story name="Appearance + tone">
    <>
      <Flex>
        <Button appearance="primary">Default</Button>
        <Button appearance="primary" tone="neutral" ml={3}>
          Neutral
        </Button>
        <Button appearance="primary" tone="positive" ml={3}>
          Positive
        </Button>
        <Button appearance="primary" tone="critical" ml={3}>
          Critical
        </Button>
        <Button appearance="primary" tone="caution" ml={3}>
          Caution
        </Button>
      </Flex>
      <Flex mt={4}>
        <Button appearance="minimal">Default</Button>
        <Button appearance="minimal" tone="neutral" ml={3}>
          Neutral
        </Button>
        <Button appearance="minimal" tone="positive" ml={3}>
          Positive
        </Button>
        <Button appearance="minimal" tone="critical" ml={3}>
          Critical
        </Button>
        <Button appearance="minimal" tone="caution" ml={3}>
          Caution
        </Button>
      </Flex>
    </>
  </Story>
</Preview>

## With icons

<Preview>
  <Story name="With icons">
    <>
      <Flex>
        <Button iconAfter={<EmailIcon />} size="large">
          Send
        </Button>
        <Button iconAfter={<EmailIcon />} ml={3}>
          Send
        </Button>
        <Button iconAfter={<EmailIcon />} ml={3} size="small">
          Send
        </Button>
        <Button iconBefore={<DeleteIcon />} tone="critical" ml={4} size="large">
          Delete
        </Button>
        <Button iconBefore={<DeleteIcon />} tone="critical" ml={3}>
          Delete
        </Button>
        <Button iconBefore={<DeleteIcon />} tone="critical" ml={3} size="small">
          Delete
        </Button>
      </Flex>
      <Flex mt={4}>
        <Button
          iconBefore={<MenuIcon />}
          size="large"
        >
          Menu
        </Button>
        <Button
          iconBefore={<MenuIcon />}
          ml={3}
        >
          Menu
        </Button>
        <Button
          iconBefore={<MenuIcon />}
          ml={3}
          size="small"
        >
          Menu
        </Button>
        <Button
          iconBefore={<MenuIcon />}
          appearance="primary"
          ml={4}
          size="large"
        >
          Menu
        </Button>
        <Button iconBefore={<MenuIcon />} appearance="primary" ml={3}>
          Menu
        </Button>
        <Button
          iconBefore={<MenuIcon />}
          appearance="primary"
          ml={3}
          size="small"
        >
          Menu
        </Button>
      </Flex>
      <Flex mt={4}>
        <Button iconAfter={<ExpandBottomIcon />} appearance="minimal">
          Select
        </Button>
        <Button
          iconBefore={<RefreshIcon />}
          iconColor={baseTheme.colors.blue.base}
          ml={3}
          size="small"
          appearance="minimal"
          tone="neutral"
        >
          Refresh
        </Button>
      </Flex>
    </>
  </Story>
</Preview>

## Icon only

<Preview>
  <Story name="Icon only">
    <Flex>
      <IconButton>
        <MenuIcon />
      </IconButton>
      <IconButton
        appearance="minimal"
        tone="critical"
        ml={4}
      >
        <DeleteIcon />
      </IconButton>
      <IconButton appearance="primary" ml={4}>
        <EmailIcon />
      </IconButton>
    </Flex>
  </Story>
</Preview>

<Story name="Playground" decorators={[withKnobs]}  parameters={{ docs: { disable: true } }}>
  <Flex>
    <Button
      tone={radios(
        "tone",
        {
          default: "default",
          neutral: "neutral",
          positive: "positive",
          critical: "critical",
          caution: "caution",
        },
        "default",
      )}
      size={radios(
        "size",
        {
          small: "small",
          medium: "medium",
          large: "large",
        },
        "medium",
      )}
      appearance={radios(
        "appearance",
        {
          default: "default",
          primary: "primary",
          minimal: "minimal",
        },
        "default",
      )}
    >
      {text("Text", "Delete")}
    </Button>
    <Button
      iconBefore={<DeleteIcon />}
      ml={4}
      tone={radios(
        "tone",
        {
          default: "default",
          neutral: "neutral",
          positive: "positive",
          critical: "critical",
          caution: "caution",
        },
        "default",
      )}
      size={radios(
        "size",
        {
          small: "small",
          medium: "medium",
          large: "large",
        },
        "medium",
      )}
      appearance={radios(
        "appearance",
        {
          default: "default",
          primary: "primary",
          minimal: "minimal",
        },
        "default",
      )}
    >
      {text("Text", "Delete")}
    </Button>
    <Button
      iconAfter={<DeleteIcon />}
      ml={4}
      tone={radios(
        "tone",
        {
          default: "default",
          neutral: "neutral",
          positive: "positive",
          critical: "critical",
          caution: "caution",
        },
        "default",
      )}
      size={radios(
        "size",
        {
          small: "small",
          medium: "medium",
          large: "large",
        },
        "medium",
      )}
      appearance={radios(
        "appearance",
        {
          default: "default",
          primary: "primary",
          minimal: "minimal",
        },
        "default",
      )}
    >
      {text("Text", "Delete")}
    </Button>
  </Flex>
</Story>
