import { Story, Meta, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, radios, text } from "@storybook/addon-knobs";
import {
  EmailIcon,
  DeleteIcon,
  MenuIcon,
  ExpandBottomIcon,
  RefreshIcon,
} from "../helpers/icons";
import { Stack, Text, Button, IconButton } from "../src";
import { tokens } from "../src/theme.ts";

<Meta title="MDX|Button" component={Button} />

# Button

<Props of={Button} exclude="theme" />

## Appearances

<Preview>
  <Story name="Appearance">
    <Stack gap={3}>
      <Button>Primary (default)</Button>
      <Button appearance="secondary">Secondary</Button>
      <Button appearance="minimal">Minimal</Button>
    </Stack>
  </Story>
</Preview>

## Size

<Preview>
  <Story name="Size">
    <Stack gap={3}>
      <Button size="large">Large</Button>
      <Button>Medium (default)</Button>
      <Button size="small">Small</Button>
    </Stack>
  </Story>
</Preview>

## Tone

<Preview>
  <Story name="Tone">
    <Stack gap={3}>
      <Button>Info (default)</Button>
      <Button tone="neutral">Neutral</Button>
      <Button tone="positive">Positive</Button>
      <Button tone="critical">Negative</Button>
      <Button tone="caution">Caution</Button>
    </Stack>
  </Story>
</Preview>

## Mixing appearance and tone

<Preview>
  <Story name="Appearance + tone">
    <Stack direction="vertical" gap={4}>
      <Stack gap={3}>
        <Button>Default</Button>
        <Button tone="neutral">Neutral</Button>
        <Button tone="positive">Positive</Button>
        <Button tone="critical">Critical</Button>
        <Button tone="caution">Caution</Button>
      </Stack>
      <Stack gap={3}>
        <Button appearance="secondary">Default</Button>
        <Button appearance="secondary" tone="neutral">
          Neutral
        </Button>
        <Button appearance="secondary" tone="positive">
          Positive
        </Button>
        <Button appearance="secondary" tone="critical">
          Critical
        </Button>
        <Button appearance="secondary" tone="caution">
          Caution
        </Button>
      </Stack>
      <Stack gap={3}>
        <Button appearance="minimal">Default</Button>
        <Button appearance="minimal" tone="neutral">
          Neutral
        </Button>
        <Button appearance="minimal" tone="positive">
          Positive
        </Button>
        <Button appearance="minimal" tone="critical">
          Critical
        </Button>
        <Button appearance="minimal" tone="caution">
          Caution
        </Button>
      </Stack>
    </Stack>
  </Story>
</Preview>

## With icons

<Preview>
  <Story name="With icons">
    <Stack direction="vertical" gap={6}>
      <Stack gap={3}>
        <Button iconAfter={<EmailIcon />} size="large">
          Send
        </Button>
        <Button iconAfter={<EmailIcon />}>
          Send
        </Button>
        <Button iconAfter={<EmailIcon />} size="small">
          Send
        </Button>
        <Button appearance="secondary" iconBefore={<DeleteIcon />} tone="critical" ml={4} size="large">
          Delete
        </Button>
        <Button appearance="secondary" iconBefore={<DeleteIcon />} tone="critical">
          Delete
        </Button>
        <Button appearance="secondary" iconBefore={<DeleteIcon />} tone="critical" size="small">
          Delete
        </Button>
      </Stack>
      <Stack gap={3}>
        <Button
          iconBefore={<MenuIcon />}
          appearance="secondary"
          tone="neutral"
          size="large"
        >
          Menu
        </Button>
        <Button
          iconBefore={<MenuIcon />}
          appearance="secondary"
          tone="neutral"
        >
          Menu
        </Button>
        <Button
          iconBefore={<MenuIcon />}
          appearance="secondary"
          tone="neutral"
          size="small"
        >
          Menu
        </Button>
        <Button
          iconBefore={<MenuIcon />}
          appearance="primary"
          size="large"
        >
          Menu
        </Button>
        <Button iconBefore={<MenuIcon />} appearance="primary">
          Menu
        </Button>
        <Button
          iconBefore={<MenuIcon />}
          appearance="primary"
          size="small"
        >
          Menu
        </Button>
      </Stack>
      <Stack gap={3}>
        <Button iconAfter={<ExpandBottomIcon />} appearance="minimal" size="small">
          Select
        </Button>
        <Button
          iconBefore={<RefreshIcon />}
          iconColor={tokens.colors.blues["500"]}
          size="small"
          appearance="minimal"
          tone="neutral"
        >
          Refresh
        </Button>
      </Stack>
    </Stack>
  </Story>
</Preview>

## Icon only

<Preview>
  <Story name="Icon only">
    <Stack gap={3}>
      <IconButton>
        <MenuIcon />
      </IconButton>
      <IconButton appearance="secondary" tone="neutral">
        <EmailIcon />
      </IconButton>
      <IconButton
        appearance="minimal"
        tone="critical"
      >
        <DeleteIcon />
      </IconButton>
    </Stack>
  </Story>
</Preview>

<Story name="Playground" decorators={[withKnobs]}  parameters={{ docs: { disable: true } }}>
  <Stack gap={3}>
    <Button
      tone={radios(
        "tone",
        {
          default: "default",
          neutral: "neutral",
          positive: "positive",
          critical: "critical",
          caution: "caution",
        },
        undefined,
      )}
      size={radios(
        "size",
        {
          small: "small",
          medium: "medium",
          large: "large",
        },
        undefined,
      )}
      appearance={radios(
        "appearance",
        {
          default: "default",
          primary: "primary",
          minimal: "minimal",
        },
        undefined,
      )}
    >
      {text("Text", "Delete")}
    </Button>
    <Button
      iconBefore={<DeleteIcon />}
      ml={4}
      tone={radios(
        "tone",
        {
          default: "default",
          neutral: "neutral",
          positive: "positive",
          critical: "critical",
          caution: "caution",
        },
        "default",
      )}
      size={radios(
        "size",
        {
          small: "small",
          medium: "medium",
          large: "large",
        },
        "medium",
      )}
      appearance={radios(
        "appearance",
        {
          default: "default",
          primary: "primary",
          minimal: "minimal",
        },
        "default",
      )}
    >
      {text("Text", "Delete")}
    </Button>
    <Button
      iconAfter={<DeleteIcon />}
      ml={4}
      tone={radios(
        "tone",
        {
          "info (default)": "info",
          neutral: "neutral",
          positive: "positive",
          critical: "critical",
          caution: "caution",
        },
        undefined,
      )}
      size={radios(
        "size",
        {
          small: "small",
          medium: "medium",
          large: "large",
        },
        "medium",
      )}
      appearance={radios(
        "appearance",
        {
          default: "default",
          primary: "primary",
          minimal: "minimal",
        },
        "default",
      )}
    >
      {text("Text", "Delete")}
    </Button>
  </Stack>
</Story>
