import { Story, Meta, Preview, Props } from "@storybook/addon-docs/blocks";
import { Spinner, Stack } from "../src";

<Meta title="MDX|Spinner" component={Spinner} />

# Spinner

<Props of={Spinner} exclude="theme" />

## Sizes

<Preview>
  <Story name="Sizes">
    <Stack gap={3}>
      <Spinner size="small" />
      <Spinner />
      <Spinner size="large" />
    </Stack>
  </Story>
</Preview>
