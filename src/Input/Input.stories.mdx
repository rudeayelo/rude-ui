import { Story, Meta, Preview, Props } from "@storybook/addon-docs/blocks";
import { withKnobs, number, text, select } from "@storybook/addon-knobs";
import { Input } from "./Input";
import { Text } from "../Text";

<Meta title="MDX|Input" component={Input} />

# Input

<Props of={Input} exclude="theme" />

<Story name="Playground" decorators={[withKnobs]}>
  <Input
    placeholder={text("Placeholder", "Placeholder")}
    fontWeight={number("Weight", 1, {
      min: 0,
      max: 2,
      step: 1,
    })}
    fontSize={number("Size", 2, {
      min: 0,
      max: 8,
      step: 1,
    })}
    color={select(
      "Color",
      {
        "blue.base": "blue.base",
        "blue.lightest": "blue.lightest",
        "blue.light": "blue.light",
        "blue.dark": "blue.dark",
        "blue.darkest": "blue.darkest",
        "green.base": "green.base",
        "green.lightest": "green.lightest",
        "green.light": "green.light",
        "green.dark": "green.dark",
        "green.darkest": "green.darkest",
        "orange.base": "orange.base",
        "orange.lightest": "orange.lightest",
        "orange.light": "orange.light",
        "orange.dark": "orange.dark",
        "orange.darkest": "orange.darkest",
        "red.base": "red.base",
        "red.lightest": "red.lightest",
        "red.light": "red.light",
        "red.dark": "red.dark",
        "red.darkest": "red.darkest",
        "g.10": "g.10",
        "g.20": "g.20",
        "g.30": "g.30",
        "g.40": "g.40",
        "g.50": "g.50",
        "g.60": "g.60",
        "g.70": "g.70",
        "g.80": "g.80",
        "g.90": "g.90",
        "g.95": "g.95",
      },
      "g.10",
    )}
    borderRadius={number("Border radius", 1, {
      min: 0,
      max: 4,
      step: 1,
    })}
    p={number("Padding", null, { min: 0, step: 1 })}
    pt={number("Padding top", null, { min: 0, step: 1 })}
    pr={number("Padding right", null, { min: 0, step: 1 })}
    pb={number("Padding bottom", null, { min: 0, step: 1 })}
    pl={number("Padding left", null, { min: 0, step: 1 })}
    m={number("Margin", null, { min: 0, step: 1 })}
    mt={number("Margin top", null, { step: 1 })}
    mr={number("Margin right", null, { step: 1 })}
    mb={number("Margin bottom", null, { step: 1 })}
    ml={number("Margin left", null, { step: 1 })}
  />
</Story>
